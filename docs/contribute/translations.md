Translations live in this repository:

- Frontend: `vue3/src/locales/`
- Backend: `cookbook/locale/` and `recipes/locale/`

To add or update a language, edit the locale files and open a pull request.

You can generate new backend language files with `manage.py makemessages -l <language_code> -i venv`.
